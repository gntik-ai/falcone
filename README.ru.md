<div align="center">
  <img src="./logo.svg" alt="Falcone" width="420" />

  <h1>Falcone</h1>

  <strong>Мультитенантная платформа Backend-as-a-Service (BaaS).</strong>

  <p>Базы данных, хранилище, аутентификация, события, реальное время и serverless-функции — изолированные по тенанту, управляемые планами и квотами, за единым API.</p>

  <p>
    <img alt="Статус: ранняя разработка" src="https://img.shields.io/badge/status-early%20development-orange" />
    <img alt="Не готово к продакшену" src="https://img.shields.io/badge/production-not%20ready-critical" />
    <img alt="Лицензия: MIT" src="https://img.shields.io/badge/license-MIT-blue" />
  </p>

  <sub>

  [English](./README.md) ·
  [Español](./README.es.md) ·
  [Français](./README.fr.md) ·
  [Deutsch](./README.de.md) ·
  [中文](./README.zh.md) ·
  **Русский**

  </sub>
</div>

---

> [!WARNING]
> **Falcone не готов к продакшену.** Проект находится в ранней, активной разработке.
> Публичные API, схемы данных и поведение во время выполнения могут измениться в любой момент — без
> предупреждения и без пути миграции. На этом этапе **нет никаких гарантий стабильности,
> безопасности или поддержки**, и проект не проходил аудит безопасности.
> **Не запускайте Falcone для продакшен-нагрузок и не доверяйте ему чувствительные данные.**
> Используйте его только для оценки, экспериментов и разработки.

---

## Принцип, лежащий в основе Falcone

Почти каждому продукту нужна одна и та же backend-инфраструктура: база данных,
файловое хранилище, аутентификация пользователей, фоновые задачи, шина событий,
обновления в реальном времени. Строить и обслуживать эту инфраструктуру **один раз
на приложение — и затем заново для каждого клиента —** это именно то место, где
команды теряют время и где рождаются инциденты безопасности.

Falcone существует, чтобы решить это раз и навсегда. Это **мультитенантный BaaS**:
единая платформа, обслуживающая множество изолированных тенантов, у каждого из
которых свои данные, идентичности и ресурсы, доступные через согласованный API.

Две идеи скрепляют всю систему:

1. **Изоляция тенантов — это контракт, а не функция.**
   Каждое чтение и каждая запись ограничены по `tenant_id` (и на уровень ниже — по
   `workspace_id`). Идентичность определяется на границе из токена, передаётся как
   явный контекст через шлюз, сервисы, слой данных и фоновые задачи и применяется в
   базе данных через row-level security и схемы на каждого тенанта. Утечка между
   тенантами считается кардинальной ошибкой.

2. **Возможности выдаются по плану и применяются повсюду.**
   То, что может делать тенант — SQL, реальное время, вебхуки, функции, Kafka,
   хранилище — это пересечение его **коммерческого плана**, **профиля развёртывания**
   и **окружения**. Шлюз ограничивает маршруты по этим ключам возможностей,
   квоты ограничивают потребление по тенанту/рабочему пространству, и каждый отказ
   фиксируется в аудите.

В результате получается платформа, на которой клиент получает полноценный backend за
минуты, а оператор сохраняет единую управляемую и наблюдаемую поверхность — вместо
флота вручную собранных backend-ов.

### Как всё устроено вместе

```text
                        ┌──────────────────────────────────────────┐
   Bearer JWT  ──▶  API-шлюз (APISIX)   /v1   идемпотентность, CORS, │
                    разрешает тенант ▸ внедряет идентичность, corr-id │
                        └───────────────┬──────────────────────────┘
                                        ▼
                        ┌──────────────────────────────────────────┐
                        │ control-plane  — 250+ REST-эндпоинтов     │
                        │ tenants · workspaces · auth/IAM · pg ·    │
                        │ mongo · storage · events · functions ·    │
                        │ metrics · plans · quotas · backup ·       │
                        │ flows (/v1/flows) · MCP (/v1/mcp) [Prev.] │
                        └───────────────┬──────────────────────────┘
            ┌───────────────────────────┼─────────────────────────────┐
            ▼                           ▼                             ▼
   provisioning-orchestrator   realtime-gateway / webhook-engine   cdc-bridges
   (саги, appliers)            scheduling-engine / backup-status   (pg & mongo → Kafka)
                               workflow-worker (интерпретатор Flows)
            │                           │                             │
            ▼                           ▼                             ▼
   ┌────────────────────────────────────────────────────────────────────────┐
   │ PostgreSQL (RLS + схема на тенант) · MongoDB · Kafka · S3/MinIO ·        │
   │ Vault (секреты) · Keycloak (realm на тенант + OAuth 2.1 для MCP) ·       │
   │ Temporal (движок Flows) · Knative (functions + runtime MCP на тенант)    │
   └────────────────────────────────────────────────────────────────────────┘
```

Платформа — это **монорепозиторий pnpm + Turbo** из сервисов на Node.js (ES-модули)
и веб-консоли на React + Vite, разворачиваемый с помощью Helm в Kubernetes и
расположенный за шлюзом APISIX.

---

## Создан для ИИ: BaAIS

Falcone начинается там же, где и любая backend-платформа — мультитенантные данные, аутентификация,
хранилище, события и функции за единым API — и направляет всё это на то, как программное обеспечение
всё чаще создаётся и эксплуатируется: **ИИ-агентами и для ИИ-агентов.**

Мы называем эту категорию **BaAIS** — *Backend-as-an-AI-Service*, игра слов на «BaaS» для мира,
нативного для ИИ. (Расшифровка намеренно нестрогая; важно направление, а не аббревиатура.) Конкретно
«создан для ИИ» означает, что backend тенанта спроектирован так, чтобы быть **нативно потребляемым
агентами**, а не только кодом приложения:

- **Хостинг MCP-серверов** *(Preview)* — тенант выставляет свой backend (данные, хранилище,
  функции) как сервер [Model Context Protocol](https://modelcontextprotocol.io), чтобы любой
  совместимый с MCP агент мог обнаружить и вызвать его в рамках собственной изоляции, аутентификации
  и квот этого тенанта. API управления обслуживается вживую по `/v1/mcp`; Instant MCP и официальный
  сервер работают сквозным образом.
- **Агентные рабочие процессы** *(Preview)* — основанный на Temporal движок **Flows** позволяет тенантам
  определять долгоживущие многошаговые рабочие процессы на DSL с JSON Schema, с каталогом
  активностей первой стороны, чьи учётные данные ограничены по тенанту — надёжная основа, которая
  нужна агенту, чтобы действовать между сервисами.

Всё, к чему прикасается агент, остаётся внутри того же контракта, что и остальная часть платформы:
ограничено по тенанту и рабочему пространству, управляется возможностями плана и аудируется.

---

## Дорожная карта

Falcone находится до версии 1.0 и быстро развивается; это направление на ближайшую перспективу, а не
обязательство.

**Доступно (Preview).** Обе флагманские AI-возможности уже приземлились и задокументированы; они
остаются Preview в рамках указанного выше статуса «не готово к продакшену»:

- **Хостинг MCP-серверов** — API управления обслуживается вживую по `/v1/mcp`; **Instant MCP** и
  **официальный сервер** работают сквозным образом (создать → курировать → опубликовать → вызвать →
  наблюдать), с изоляцией по тенанту, OAuth, квотами, реестром/версионированием и аудитом. Состояние
  сервера сейчас в памяти (одна реплика). ([эпик #386](https://github.com/gntik-ai/falcone/issues/386))
- **Flows — движок долгоживущих рабочих процессов (Temporal)** — определяемые тенантом рабочие
  процессы через DSL на JSON Schema и worker-интерпретатор, каталог активностей первой стороны с
  учётными данными, ограниченными по тенанту, триггеры и визуальный конструктор.
  ([эпик #355](https://github.com/gntik-ai/falcone/issues/355))

**В работе / запланировано.**

- **Следующие шаги MCP** — долговечный (на Postgres) многорепличный реестр серверов; кастомный
  хостинг (собственный образ) на живом пути создания; подключение workflows-as-MCP-tools; и прямое
  MCP-соединение на сервер (сегодня control-plane посредничает при вызовах инструментов).
- **Альтернативы объектного хранилища / документной БД** — *запланировано, на оценке, ещё не
  реализовано.* Платформа использует **MinIO** (объектное хранилище) и **MongoDB** (документный API);
  source-available / более лёгкие альтернативы (например, SeaweedFS; FerretDB поверх
  DocumentDB-совместимого backend) оцениваются и заменяемы на уровне развёртывания.
- **К первому стабильному релизу** — *запланировано.* Аудит безопасности, гарантии стабильности
  API/схем и инструменты миграции (см. предупреждение вверху).

---

## Возможности

| Область | Что это даёт тенанту |
| --- | --- |
| **Жизненный цикл тенанта** | Создание, приостановка, мягкое удаление и полная очистка тенантов через защищённую машину состояний (`draft → provisioning → active → suspended → soft_deleted`), с панелями управления и двойным подтверждением для разрушительных действий. |
| **Сага провижининга** | Асинхронная оркестрация, которая разворачивает (или сворачивает) тенант во всех областях — realm IAM, namespace Kafka, схема Postgres, MongoDB, namespace хранилища, namespace функций — с предварительными проверками и откатом при сбое. |
| **Рабочие пространства (workspaces)** | Границы суб-тенанта со своим slug, окружением, областью IAM и членством. Клонирование рабочих пространств по явным политикам; разрешение наследования общих vs. специализированных ресурсов. |
| **Аутентификация и IAM** | Вход в консоль через делегирование OIDC, регистрация с состоянием ожидания активации, восстановление пароля. Администрирование Keycloak (realm на тенант) для realm-ов, клиентов, ролей, scope-ов и пользователей. Проверка JWT через кэшированный JWKS с откатом на introspection. |
| **Сервисные аккаунты и OAuth2-приложения** | OAuth2-клиенты и сервисные аккаунты с API-ключами на рабочее пространство, с проверкой HTTPS redirect-URI и лимитами по плану. |
| **PostgreSQL** | Ограниченный по тенанту data API плюс администрирование/управление, захват изменений (CDC), метрики и аудит. Изоляция через row-level security (`app.tenant_id` / `app.workspace_id`) и схемы на тенант. |
| **MongoDB** | Document API на тенант/рабочее пространство, администрирование, change streams, метрики и аудит. |
| **Объектное хранилище** | S3-совместимые бакеты, multipart-загрузки, предподписанные URL, политики доступа, уведомления о событиях и квоты ёмкости на тенант. |
| **События (Kafka)** | Управление топиками и ограниченные по тенанту CDC change streams (`<префикс>.<тенант>.<пространство>`), плюс системные топики аудита/квот/жизненного цикла. |
| **Реальное время** | WebSocket-подписки (`/v1/websockets`) с аутентификацией Bearer-JWT, применением scope к каналу и изоляцией тенанта на каждую сессию. |
| **Функции** | Serverless-функции с версиями, активациями, вызовами, откатом и триггерами cron / Kafka / storage. |
| **Вебхуки** | Подписанная доставка вебхуков с повторами и защитой от SSRF (приватные, loopback, link-local и ULA диапазоны блокируются и перепроверяются в момент доставки). |
| **Планирование** | Cron-задачи с квотами на параллелизм и число задач на рабочее пространство и полным аудитом выполнения. |
| **Flows (движок рабочих процессов)** | Определяемые тенантом долгоживущие рабочие процессы на основанном на Temporal движке: DSL на JSON Schema и worker-интерпретатор, каталог активностей первой стороны с учётными данными, ограниченными по тенанту, триггеры (schedules, вебхуки, платформенные события) и визуальный конструктор в консоли. *Preview ([эпик #355](https://github.com/gntik-ai/falcone/issues/355)).* |
| **Хостинг MCP-серверов** | Хостинг MCP-серверов тенанта, чтобы ИИ-агенты вызывали backend как инструменты. API управления обслуживается вживую по `/v1/mcp`: Instant MCP (инструменты, сгенерированные из ресурса), официальный read-first сервер, обязательная курация, реестр/версионирование с проверкой против «rug-pull», OAuth 2.1, квоты/ограничения частоты по тенанту и аудит. *Preview — Instant MCP + официальный сервер вживую (состояние в памяти); хостинг собственного образа и workflows-as-tools экспериментальны ([эпик #386](https://github.com/gntik-ai/falcone/issues/386)).* |
| **Планы и квоты** | Коммерческие планы отображаются в ключи возможностей, значения квот по умолчанию и профиль развёртывания. Квоты применяют режимы hard-block / soft-grace / soft-exhausted на тенант и рабочее пространство. |
| **Резервное копирование и восстановление** | Перечисление снапшотов, оркестрация восстановления и симуляция восстановления на момент времени (PITR) поверх адаптеров S3 / Postgres / Mongo. |
| **Наблюдаемость и аудит** | Конвейер аудита на тенант (актор, конверт области, ресурс, действие, результат), передаваемый в Kafka и сохраняемый, с семействами метрик, health-проверками, дашбордами и пороговыми оповещениями. |
| **API-шлюз** | Единая публичная поверхность на `/v1` с обязательными ключами идемпотентности, correlation ID, валидацией запросов и таймаутами/повторами на маршрут. |
| **Веб-консоль** | Админ-интерфейс на React + Vite для тенантов, рабочих пространств, участников, баз данных, хранилища, функций, событий, планов, квот и наблюдаемости. |

---

## Быстрый старт с Docker Compose

Репозиторий поставляет Compose-стек, который поднимает **реальные backing-сервисы**,
с которыми работает Falcone — PostgreSQL, Keycloak, Redpanda (Kafka), MongoDB
(однонодовый replica set), MinIO (S3) и Vault — плюс шлюз APISIX и action runner.
Это самый быстрый способ получить рабочее окружение на вашей машине.

### Требования

- Docker с плагином Compose (`docker compose`)
- Node.js 20+ и `pnpm` (через `corepack enable`) — нужны только для запуска наборов тестов

### 1. Клонировать и установить

```bash
git clone https://github.com/gntik-ai/falcone.git
cd falcone
corepack enable
pnpm install
```

### 2. Поднять стек с Docker Compose

Вспомогательный скрипт настраивает за вас health-проверки, миграции, replica set
Mongo, бакет MinIO и устройство аудита Vault:

```bash
cd tests/env
./up.sh
```

…или управляйте Compose напрямую, если нужны только контейнеры:

```bash
docker compose -f tests/env/docker-compose.yml up -d --build
docker compose -f tests/env/docker-compose.yml ps
```

### 3. Сервисы и порты

| Сервис | URL / эндпоинт | Учётные данные |
| --- | --- | --- |
| API-шлюз (APISIX) | <http://localhost:9080> | Bearer JWT из Keycloak |
| Keycloak (IdP) | <http://localhost:8081> | `admin` / `admin` |
| PostgreSQL | `localhost:55432` | `falcone` / `falcone` |
| MongoDB (rs0) | `localhost:57017` | — |
| Redpanda (Kafka) | `localhost:19092` | — |
| MinIO (S3 API) | <http://localhost:59000> | `minioadmin` / `minioadmin` |
| Консоль MinIO | <http://localhost:59001> | `minioadmin` / `minioadmin` |
| Vault (dev) | <http://localhost:58200> | токен `root` |

### 4. Проверить в деле

```bash
# Запустить наборы unit / contract / e2e против живого стека
pnpm test

# или контрактный black-box набор (только публичный интерфейс)
bash tests/blackbox/run.sh
```

### 5. Остановить и удалить

```bash
cd tests/env
./down.sh
# или: docker compose -f tests/env/docker-compose.yml down -v
```

> Для полноценного продакшн-развёртывания (runtime функций, control-plane и
> веб-консоль) используйте Helm-чарты в `helm/` и `charts/` на кластере
> Kubernetes — см. манифесты в `deploy/`.

---

## Структура репозитория

```text
apps/            control-plane (поверхность REST API) · web-console (UI на React) ·
                 cli (CLI falcone: mcp init/dev/deploy) · mcp-server-sdk (SDK MCP-инструментов с привязкой к тенанту)
services/        gateway-config, realtime-gateway, webhook-engine, cdc-bridges,
                 scheduling-engine, provisioning-orchestrator, backup-status,
                 workflow-worker (интерпретатор DSL Flows), audit, adapters,
                 internal-contracts, …
charts/ helm/    Развёртывание Kubernetes / Helm (вкл. компоненты temporal, workflowWorker, mcp)
deploy/          Маршруты APISIX, bootstrap для kind/OpenShift
tests/           blackbox (контракт) · e2e (Playwright, вкл. спеки mcp) · env (стек Compose)
openspec/        Рабочий процесс изменений, управляемый спецификациями
```

---

## Стороннее ПО и лицензии

Сам Falcone распространяется под **лицензией MIT** (см. [LICENSE](./LICENSE)). Он построен на
перечисленном ниже стороннем ПО. Компоненты с пометкой ⚠ являются **copyleft или source-available**
(не open source по версии OSI) — см. примечание о совместимости далее.

### Платформа и инфраструктура (разворачиваются как сервисы / образы)

| Компонент | Роль в Falcone | Лицензия (SPDX) | Ссылка |
| --- | --- | --- | --- |
| PostgreSQL 16 (+ pgvector) | Основное хранилище данных тенанта; изоляция RLS + схема на тенант; pgvector для векторного поиска | `PostgreSQL` | [postgresql.org](https://www.postgresql.org/about/licence/) · [pgvector](https://github.com/pgvector/pgvector) |
| MongoDB Server 7 | Document API на тенант/рабочее пространство | ⚠ `SSPL-1.0` | [mongodb.com](https://www.mongodb.com/legal/licensing/community-edition) |
| Redpanda 24.2 | Kafka-совместимая шина событий / CDC-стриминг | ⚠ `BSL-1.1` (Redpanda) + `RCL` | [licenses](https://github.com/redpanda-data/redpanda/tree/dev/licenses) |
| MinIO | S3-совместимое объектное хранилище | ⚠ `AGPL-3.0` | [LICENSE](https://github.com/minio/minio/blob/master/LICENSE) |
| HashiCorp Vault 1.18 | Управление секретами | ⚠ `BUSL-1.1` | [LICENSE](https://github.com/hashicorp/vault/blob/main/LICENSE) |
| Keycloak 26 | IAM с realm на тенант / OIDC | `Apache-2.0` | [keycloak](https://github.com/keycloak/keycloak) |
| Apache APISIX 3.9 | API-шлюз (публичная поверхность `/v1`) | `Apache-2.0` | [apisix](https://github.com/apache/apisix) |
| Temporal (сервер 1.25 + TypeScript SDK 1.18) | Движок долгоживущих рабочих процессов за Flows | `MIT` | [temporal](https://github.com/temporalio/temporal) · [sdk-typescript](https://github.com/temporalio/sdk-typescript) |
| Knative Serving + Kourier | Среда выполнения serverless-функций | `Apache-2.0` | [serving](https://github.com/knative/serving) · [net-kourier](https://github.com/knative-extensions/net-kourier) |
| Apache OpenWhisk | Устаревший / опциональный движок функций | `Apache-2.0` | [openwhisk](https://github.com/apache/openwhisk) |
| Kubernetes + Helm | Развёртывание и оркестрация | `Apache-2.0` | [kubernetes](https://github.com/kubernetes/kubernetes) · [helm](https://github.com/helm/helm) |
| Node.js 22 | Среда выполнения сервисов | `MIT` | [nodejs](https://github.com/nodejs/node) |
| nginx | Раздача статики образа веб-консоли | `BSD-2-Clause` | [nginx.org](https://nginx.org/LICENSE) |

### Основные прикладные фреймворки и библиотеки (npm)

| Компонент | Роль в Falcone | Лицензия (SPDX) | Ссылка |
| --- | --- | --- | --- |
| React 18 | UI веб-консоли | `MIT` | [react](https://github.com/facebook/react) |
| Vite | Сборка и dev-сервер консоли | `MIT` | [vite](https://github.com/vitejs/vite) |
| TypeScript | Типизированный код (консоль, workflow worker) | `Apache-2.0` | [TypeScript](https://github.com/microsoft/TypeScript) |
| Tailwind CSS | Стили консоли | `MIT` | [tailwindcss](https://github.com/tailwindlabs/tailwindcss) |
| React Flow (`@xyflow/react`) | Холст визуального конструктора Flows | `MIT` | [xyflow](https://github.com/xyflow/xyflow) |
| Monaco Editor (+ `monaco-yaml`) | Редактирование кода / YAML в консоли | `MIT` | [monaco-editor](https://github.com/microsoft/monaco-editor) |
| node-postgres (`pg`) | Клиент PostgreSQL | `MIT` | [node-postgres](https://github.com/brianc/node-postgres) |
| MongoDB Node Driver (`mongodb`) | Клиент MongoDB | `Apache-2.0` | [node-mongodb-native](https://github.com/mongodb/node-mongodb-native) |
| KafkaJS | Клиент Kafka / Redpanda | `MIT` | [kafkajs](https://github.com/tulios/kafkajs) |
| AWS SDK for JS v3 (`@aws-sdk/client-s3`) | Клиент S3 / MinIO | `Apache-2.0` | [aws-sdk-js-v3](https://github.com/aws/aws-sdk-js-v3) |
| jose + jwks-rsa | Проверка JWT / JWKS | `MIT` | [jose](https://github.com/panva/jose) · [node-jwks-rsa](https://github.com/auth0/node-jwks-rsa) |
| ws | WebSocket-шлюз реального времени | `MIT` | [ws](https://github.com/websockets/ws) |
| Ajv | Проверка JSON Schema | `MIT` | [ajv](https://github.com/ajv-validator/ajv) |
| cel-js | Вычисление выражений возможностей / политик | `MIT` | [cel-js](https://www.npmjs.com/package/cel-js) |
| Playwright | E2E-тесты на реальном стеке | `Apache-2.0` | [playwright](https://github.com/microsoft/playwright) |

> [!IMPORTANT]
> **Совместимость лицензий — требуется проверка.** Собственный код Falcone — **MIT**, что совместимо
> с использованием всех перечисленных выше пермиссивных компонентов (MIT, Apache-2.0, ISC, BSD,
> PostgreSQL). Компоненты с пометкой ⚠ **не** являются open source по версии OSI и заслуживают
> проверки:
> - **MongoDB (`SSPL-1.0`)**, **MinIO (`AGPL-3.0`)**, **Redpanda (`BSL-1.1` + `RCL`)** и
>   **Vault (`BUSL-1.1`)** являются copyleft или source-available.
> - Запуск их как **отдельных backing-сервисов, с которыми Falcone общается по сети**, сам по себе
>   не накладывает их лицензию на MIT-код Falcone (нет линковки / производного произведения).
>   **Однако** их оговорки «предоставление как сервиса» / «конкурирующий сервис» напрямую значимы для
>   мультитенантного BaaS, который **повторно выставляет** их функциональность тенантам — Mongo
>   data API, Kafka/событийный API, S3 storage API. В частности, **§13 SSPL и §13 AGPL нацелены
>   именно на предоставление функциональности ПО в виде сервиса**, а грант BSL Redpanda/Vault
>   исключает конкурирующие управляемые предложения. Изучите эти условия перед любым хостинговым или
>   коммерческим предложением. Все четыре заменяемы на уровне развёртывания, если их условия не
>   подходят под ваш сценарий использования.

**Не исчерпывающий список.** В этой таблице перечислены **основные** сторонние компоненты, а не
полное дерево транзитивных зависимостей (мелкие утилиты — `undici`, `clsx`, `lucide-react`,
`uuid`, `cron-parser`, `js-yaml` и т. д. — опущены). Для полной картины сгенерируйте SBOM / отчёт
о лицензиях — например, `license-checker` или `pnpm licenses list` для npm-воркспейсов — и, если
позже будут добавлены компоненты на Python или Go, `pip-licenses` и `go-licenses` соответственно.
Проверьте вывод перед распространением.

---

## Лицензия

См. [LICENSE](./LICENSE).
