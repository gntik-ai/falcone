# add-write-time-auto-embedding

Add write-time auto-embedding: when a row is inserted/updated into a Postgres collection with a configured text-to-vector mapping, generate the embedding in-platform and store it in the vector column
