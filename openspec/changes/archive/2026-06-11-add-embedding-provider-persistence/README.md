# add-embedding-provider-persistence

Persist embedding-provider config in Postgres and wire embeddingExecutor into main.mjs so in-platform KNN embedding is operational and durable across restarts and replicas
