## 1. Baseline and test-first contracts

- [ ] T01: Confirm baseline green before implementation. Record the Falcone SHA/worktree and the `falcone-charts` SHA/worktree; preserve unrelated changes; run `openspec validate --all --strict`, `corepack pnpm validate:repo`, `corepack pnpm lint:md`, `corepack pnpm lint:snippets`, `corepack pnpm test:unit`, the existing webhook unit/integration/contract/black-box tests, `helm lint charts/in-falcone`, and base/kind/OpenShift `helm template` renders. Acceptance: both repositories are green at the recorded baselines or every pre-existing failure is independently documented before C-25 files change. Test target: Falcone quality suites plus chart lint/render at the pinned SHAs.

- [x] T02: Add failing strict key-format and no-fallback tests in Falcone before production changes. Cover canonical-v1 round-trip of exactly 32 bytes; wrong version/alphabet/padding/length/whitespace/trailing data/non-canonical encodings; missing/empty keys in every `NODE_ENV`; explicit-only legacy normalization; opaque ID derivation from namespace/name/key only; verification-cipher mismatch; and a repository scan proving `development-signing-key`/arbitrary runtime normalization are absent from serving paths. Acceptance: the new tests fail for the current fallback/permissive implementation and contain no real key material. Test target: `tests/unit/webhook-*.test.mjs` and a focused black-box no-leak contract.

- [x] T03: Add failing migration and lifecycle-state tests for additive migration `004`. Assert `encryption_key_id`, singleton `webhook_master_key_state`, `webhook_master_key_rotations`, uniqueness/idempotency constraints, replay safety, legacy rows left unguessed, verification ciphertext/IV (not a digest), bounded sanitized errors, and absence of key bytes/encoded keys/decrypted secrets in metadata. Acceptance: schema assertions fail before migration 004 exists and pass on empty and populated legacy fixtures after implementation. Test target: `tests/blackbox/webhook-schema-bootstrap.test.mjs` plus PostgreSQL migration integration tests.

## 2. Falcone key context, persistence, and maintenance lifecycle

- [x] T04: Implement the strict webhook master-key module under `packages/webhook-engine/src/` and update `packages/webhook-engine/src/webhook-signing.mjs` to consume an already-resolved `{keyBytes,keyId,mode}` context. Remove arbitrary-string normalization from ordinary serving and remove `development-signing-key` from `packages/webhook-engine/actions/webhook-management.mjs`, `revealSecretRecords`, delivery, retry, and all other consumers. Acceptance: ordinary runtime accepts only verified canonical-v1 context (or an explicitly established legacy serving context), never hashes malformed input, and all T02 tests pass. Test target: focused unit tests plus existing signing/management/delivery suites.

- [x] T05: Create `packages/webhook-engine/migrations/004-webhook-master-key-lifecycle.sql`, add it to `apps/control-plane/webhook-schema.mjs`/image migration packaging, and implement a platform-only lifecycle repository separate from `buildWebhookDb`. Add new writes of `webhook_signing_secrets.encryption_key_id` while preserving all existing tenant/workspace predicates and RLS/application isolation. Acceptance: migration is additive/idempotent, normal adapters cannot invoke lifecycle operations, and populated legacy fixtures remain unchanged pending adoption. Test target: migration replay/schema tests and SQL/adapter isolation assertions.

- [x] T06: Implement secret-safe key-state initialization and verification. On an empty database, atomically initialize canonical current identity plus a fresh-IV encrypted verification sentinel; on a non-empty database with missing/incomplete state, return adoption-required; verify opaque identity, mode, sentinel, row identities, and lifecycle state without storing/logging key bytes or digests. Acceptance: empty initialization is idempotent; wrong bytes at the same external identity and mixed/unlabeled rows fail closed. Test target: real-PostgreSQL integration tests for initialization, mismatch, retry, and no-leak cases.

- [x] T07: Implement the platform maintenance CLI in the Falcone image with `status`, legacy `adopt`, `rotate`, `recover`, and `finalize` actions. Keys/database passwords SHALL arrive through Secret-sourced environment/files, never CLI arguments; request and rotation IDs SHALL be idempotent and reject rebinding; status/output/errors SHALL expose only non-secret identities/state/counts/timestamps/deadline/sanitized codes. Acceptance: no HTTP route, OpenAPI entry, SDK method, tenant role, or public audit schema is added, and a constrained read-only status fixture reveals no Secret data. Test target: CLI unit/integration tests, help/output snapshots, argument/process inspection, and a no-public-route contract scan.

- [x] T08: Implement explicit legacy adoption. Quiesce/verify all webhook crypto consumers, take advisory and transaction locks, apply the exact historical 32-byte-or-SHA-256 normalization only in the authorized operation, authenticate/decrypt every row, preserve exact plaintext/non-key columns, assign the opaque key ID, and atomically commit legacy verification/serving state and ledger outcome. Acceptance: one incompatible/mixed row rolls back everything; identical request retry is a no-op; conflicting request reuse is rejected; adoption never performs canonical rotation. Test target: PostgreSQL transaction tests with multi-tenant rows, injected crypto failure, lock contention, and retry/crash fixtures.

- [x] T09: Implement canonical rotate/recover/finalize transaction flows. Require distinct source/target Secret identities; quiesce all consumers; lock; decrypt/re-encrypt every row with fresh IVs in one transaction; verify exact plaintext; preserve IDs/scopes/status/grace/revocation data; commit current/recovery verification state, counts, and deadline atomically; reconcile ambiguous commit as `recovery_required`; resume/recover idempotently; and finalize only eligible managed recovery metadata after the deadline. Acceptance: failure before commit restores source serving, ambiguity after commit serves neither key until resume/recover, same-name rotation is rejected, and early/repeated finalization is safe. Test target: real-PostgreSQL fault-injection tests at pre-lock, mid-row, pre-commit, lost-ack/post-commit, rollout, recover, and finalize boundaries.

- [x] T10: Integrate lifecycle outcomes with Falcone's existing internal audit path without changing the public audit schema. Emit action/request/rotation IDs, opaque source/target identities, state, counts/timestamps/deadline, and bounded outcome code only; scrub raw crypto/database/Kubernetes exceptions. Acceptance: each state-mutating maintenance action has a durable lifecycle ledger and sanitized internal audit outcome, with no key bytes/digests/plaintext/SQL parameters in success or failure evidence. Test target: audit schema/event unit tests and captured-log/ledger negative scans.

## 3. Falcone startup, serving, and regression safety

- [x] T11: Restructure `apps/control-plane/server.mjs` bootstrap to await schema migration, key resolution, lifecycle verification, and recovery-state checks before `server.listen`; keep readiness false on all failures. Thread the one resolved key context through `apps/control-plane/webhook-handlers.mjs`, management, reveal, delivery, retry, and later consumer seams instead of reading raw env independently. Update `apps/control-plane/Dockerfile` to package migration 004 and CLI/modules. Acceptance: missing/malformed/wrong/in-progress/recovery-required state cannot listen or become ready, and verified state starts once. Test target: startup child-process tests, readiness tests, image build/import smoke tests, and restart/idempotency tests.

- [x] T12: Prove tenant/public behavior is invariant across adoption, rotation, recovery, and finalization. Seed at least two tenants/workspaces with active/grace/revoked signing-secret rows; compare exact decrypted plaintext and deterministic signatures for the same payload before/after; exercise management and delivery paths; retain tenant/workspace SQL predicates, no-existence disclosure, authorization, `WEBHOOK_MAX_SUBSCRIPTIONS_PER_WORKSPACE`, per-subscription rotation, statuses, IDs, timestamps, and API response schemas. Acceptance: no cross-tenant row is loaded/modified, P12 signatures remain byte-identical, P13 access remains denied/not-found, and public contracts/quotas do not drift. Test target: webhook contract/integration/black-box tenant-isolation and quota suites plus lifecycle fixtures.

- [ ] T13: Build and publish the compatible Falcone control-plane image before chart consumption. Verify the image contains the strict parser, migration 004, lifecycle repository/CLI, startup gate, and no fallback; declare the compatible chart minimum and migration/recovery notes without deploying the image alone into an unsafe legacy chart revision. Acceptance: image smoke tests start with a valid referenced key and fail before listen for missing/malformed/incompatible state; image/build logs and SBOM/evidence contain no key. Test target: Docker build, import/smoke, filesystem/package assertions, and secret-pattern scans.

## 4. falcone-charts values, templates, credentials, and RBAC

- [x] T14: In `falcone-charts`, add the complete `global.webhookSigningKey` defaults/schema and cross-field validation; reject unknown/inline fields, invalid enums/combinations, absent action IDs, invalid recovery windows, same source/target identities, and the reserved `WEBHOOK_SIGNING_KEY` name in every chart-inspectable control-plane env/config route (`controlPlane.env`, `global.transportSecurity.env`, and `controlPlane.config.inline`). Preserve opaque external envFrom references that Helm cannot inspect. Remove literal/deprecated key declarations from `deploy/kind/values-kind.yaml`, `deploy/helm/webhook-engine-values.yaml`, profiles/examples, and other tracked overrides. Acceptance: valid managed/external/adopt/rotate/recover/finalize values pass; every negative matrix entry fails without a partial manifest or supplied-value disclosure. Test target: schema validation, `helm lint`, invalid-values unit/render cases, and repository literal scans.

- [x] T15: Update the aliased `component-wrapper` workload template and umbrella helpers so only `wrapper.componentId=control-plane` receives exactly one required `WEBHOOK_SIGNING_KEY` `valueFrom.secretKeyRef` (`optional:false`), non-secret `WEBHOOK_SIGNING_KEY_MODE`/opaque `WEBHOOK_SIGNING_KEY_ID`, and lifecycle rollout annotations. Acceptance: all other aliases are unchanged, user env cannot duplicate/override the reserved entry, and rendered workload/ConfigMaps contain no bytes or key-derived digest. Test target: base/profile/kind/OpenShift/air-gap renders, structural YAML assertions, and exact-one-env-entry tests.

- [x] T16: Add ordered pre-install/pre-upgrade credential and lifecycle hook Jobs, dedicated ServiceAccounts, and namespace-scoped least-privilege RBAC. The credential phase SHALL generate canonical-v1 inside the cluster for a fresh managed install or explicit new rotation target, create immutable retained ownership-labeled Secrets without update/patch permission, and never regenerate a missing ordinary-upgrade current key. The later phase SHALL consume existing source/target/database credentials only through required Secret refs, perform read-only external validation, invoke adopt/rotate/recover/finalize, quiesce/restore chart-owned consumers, and delete only an eligible managed recovery Secret during explicit finalization. Acceptance: external Secrets are never claimed/mutated/deleted; current/recovery managed Secrets survive upgrade/uninstall; Jobs run under restricted Kubernetes/OpenShift security contexts; no key appears in args/Events/output. Test target: chart unit/render/RBAC verb tests, hook-weight/order tests, fake-API integration, uninstall retention, and external-resource before/after comparisons.

- [x] T17: Add comprehensive chart test fixtures for fresh managed install, ordinary no-op upgrade, valid external Secret, explicit legacy adoption, separate canonical rotation, recover, finalize, invalid/missing/malformed Secret, same-name external mutation, request/rotation-ID conflicts, and retained recovery. Acceptance: render and hook tests assert fail-closed behavior, byte reuse, immutable/new-name rotation, no-leak surfaces, and consistent Kubernetes/OpenShift results. Test target: `helm lint`, `helm template` matrix, chart CI tests, and negative scans covering manifests, values, NOTES, hooks, annotations, commands, and Events.

## 5. Coordinated release, pin, and disposable-cluster verification

- [ ] T18: Release the chart only after the compatible Falcone image is available; bump chart/app compatibility metadata and release notes, then update Falcone `.github/workflows/ci.yml` `FALCONE_CHARTS_REF` from the audited chart SHA to the reviewed release commit. Run Falcone chart contract/unit tests against that exact pin and verify install values select the compatible image. Acceptance: no branch/repository points at an incompatible chart-image pair and the documented sequence is image → chart → Falcone pin → environment adoption → later rotation/finalization. Test target: both CI pipelines, pinned checkout identity assertion, chart render contracts, and image-reference checks.

- [ ] T19: Verify on a disposable kind cluster without reusing or mutating the audited environment. Exercise fresh generation (prove format/length only inside a secret-safe assertion), restart/no-op upgrade with exact byte reuse, seeded multi-tenant legacy adoption, separate canonical rotation, public signature/plaintext invariance, pre-commit rollback, post-commit ambiguity/resume/recover, uninstall retention, and finalization. Capture only field-selected/redacted evidence and always tear down workloads/namespaces. Acceptance: all lifecycle/failure/isolation/no-leak scenarios pass and cleanup proof contains no Secret data. Test target: dedicated real-stack/e2e suite plus secret-pattern scan of evidence.

- [ ] T20: Verify the same reference lifecycle on a disposable OpenShift or OpenShift-compatible cluster under restricted security constraints. Confirm hook/runtime ServiceAccounts, non-root security contexts, namespace RBAC, immutable/external Secret handling, quiesce/rollout, readiness fail-closed behavior, recovery, retention, and cleanup. Acceptance: no privileged SCC/cluster-wide RBAC or platform-specific workaround is required, external resources remain byte-for-byte unchanged, and secret-safe evidence/teardown are complete. Test target: OpenShift render validation plus live install/upgrade/rotate/recover/finalize smoke suite.

## 6. Documentation, explicit non-applicability, and independent checks

- [ ] T21: Write detailed reference and runbook documentation in both repositories. Document the complete values contract, managed/external custody, secret-safe provisioning (never `--set`, rendered files, shell history, or evidence), maintenance-window/drain steps, preflight/backup and matching-key custody, explicit adoption then later canonical rotation, retry/recovery/finalization, external-manager in-place mutation risk, Helm-history disclosure and rollback hazard, forward fixed-chart recovery, restore/key coupling, observability/status checks for P4/P10, and Kubernetes Secret/etcd/backup/node/`exec` operator responsibilities. Acceptance: P17 can execute fresh install, upgrade, rotation, recovery, and finalization without source archaeology or exposing a value. Test target: docs lint/snippets plus a literal runbook rehearsal on a disposable cluster with a second reviewer.

- [x] T22: Mark UI/accessibility and public wire work explicitly not applicable. Record that this bounded fix adds no web-console surface; therefore no visual/a11y interaction changes apply, and no OpenAPI, SDK, gateway, public-auth, Kafka, public audit-schema, tenant-role, KMS/ESO/Vault, or general secret-controller work is authorized. Acceptance: contract/repository diffs contain none of those surfaces except documentation stating non-applicability. Test target: path-scoped diff review and public route/OpenAPI/SDK/gateway/Kafka schema snapshots unchanged.

- [ ] T23: Run independent maker/checker reviews. A security checker SHALL inspect crypto parsing, zero fallback, no-leak surfaces, Secret/RBAC custody, external ownership, idempotency, commit ambiguity, history/rollback, and destructive finalization; a contract/isolation checker SHALL verify public wire/quotas/authz/tenant predicates/signatures are unchanged; a deployment reviewer SHALL verify alias/global Helm behavior, hook ordering, restricted Kubernetes/OpenShift operation, retention, and cross-repository pinning. Acceptance: each checker receives only task, diff, and reproducible evidence (not maker self-assessment), and all confirmed findings are fixed or explicitly block completion. Test target: independent review reports tied to exact Falcone/image/chart SHAs.

- [ ] T24: Update the C-25/DEVOPS-08 audit finding and capability/remediation matrix only after independent live confirmation. Link secret-safe evidence showing one required reference, no literal in current artifacts, byte-preserving adoption/rotation, fail-closed recovery, P4 posture visibility, and preserved tenant/public behavior; do not copy key material or broad Secret/workload dumps into the ledger. Acceptance: the matrix closes only C-25 with exact SHA/image/chart/environment identity and retains stated custody boundaries/history risk. Test target: audit-ledger validation and evidence secret-pattern scan.

- [ ] T25: Run final repository and OpenSpec gates after implementation. In Falcone run targeted lifecycle/transaction/startup/tenant/public suites, full unit/contracts/black-box/e2e gates, `corepack pnpm validate:repo`, Markdown/snippet lint, and `openspec validate fix-audit-c25-webhook-signing-key-lifecycle --strict`; in `falcone-charts` run schema tests, chart unit tests, `helm lint`, all render matrices, and disposable-cluster tests. Acceptance: every gate is green, exact modified paths and residual operator risks are documented, and archival occurs only after both repository changes and independently reviewed release/pin evidence are complete. Test target: final CI artifacts for both repositories plus strict OpenSpec validation.
