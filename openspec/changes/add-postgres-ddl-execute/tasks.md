## 1. Baseline

- [ ] 1.1 Confirm baseline green: `bash tests/blackbox/run.sh`
- [ ] 1.2 Confirm `openspec validate add-postgres-ddl-execute --strict` passes

## 2. Black-box tests against tests/env (write first — red-green discipline)

- [ ] 2.1 Spin up `tests/env` (real Postgres + Keycloak) and confirm it is reachable
- [ ] 2.2 Write test: `POST .../tables` with `executionMode: "execute"` → 2xx; subsequent SELECT confirms table exists and is queryable
- [ ] 2.3 Write test: `POST .../columns` with `executionMode: "execute"` → column present in subsequent query
- [ ] 2.4 Write test: `POST .../indexes` with `executionMode: "execute"` → index visible in `pg_indexes`
- [ ] 2.5 Write test: DDL with invalid type/duplicate name → rolled back, 4xx/5xx response with sanitized error (no stack trace, no raw SQL fragment)
- [ ] 2.6 Write test: sanitized error body does not contain Node.js stack trace or `pg` driver properties
- [ ] 2.7 Write test: raw SQL `POST .../admin/{db}/sql` with `pln_01starter` → 403 with `postgres.admin_sql` flag indication
- [ ] 2.8 Write test: raw SQL with `pln_01regulated` and a clean statement → 2xx execution result
- [ ] 2.9 Write test: raw SQL with `SET ROLE` in body (forbidden pattern, `postgresql-admin.mjs:880`) → rejected before execution
- [ ] 2.10 Write test: create RLS policy with `rlsEnabled: true` + `executionMode: "execute"` → `pg_policies` shows policy; `pg_class.relrowsecurity` = `t`
- [ ] 2.11 Write test: policy body containing DDL clause (blocked by `postgresql-governance-admin.mjs:187`) → 4xx validation error before SQL reaches DB
- [ ] 2.12 Confirm all new tests fail before implementation

## 3. Executor implementation

- [ ] 3.1 Create `apps/control-plane/src/postgres-ddl-executor.mjs`
  - Accept adapter-call envelope (output of `buildPostgresAdminAdapterCall`)
  - Acquire `pg.Client` from connection registry by `workspace_id + databaseName`
  - Branch on `ddlPlan.transactionMode`:
    - `'non_transactional_ddl'` → run statements without `BEGIN`/`COMMIT`
    - all others → `BEGIN` → statements in order → `COMMIT` on success / `ROLLBACK` on error
  - Return adapter-call result with `executionMode: "execute"` on success
  - On Postgres error: rollback (if transactional), map to sanitized `{ code: "POSTGRES_DDL_ERROR", message, hint }` — strip stack and raw SQL
- [ ] 3.2 Wire RLS path: when `ddlPlan` already contains `ALTER TABLE … ENABLE ROW LEVEL SECURITY` (generated by `postgresql-governance-admin.mjs:547,557`), confirm it runs in the same transaction — no additional logic needed
- [ ] 3.3 Update `apps/control-plane/src/postgres-admin.mjs` route handlers: call executor when `executionMode !== 'preview'`; pass through preview response as before when `executionMode === 'preview'`
- [ ] 3.4 Wire executor into the generic pipeline from `add-control-plane-executor`

## 4. Plan-tier gate preservation

- [ ] 4.1 Verify `resolvePostgresAdminSqlPolicy` (`postgresql-admin.mjs:801`) is called in the raw-SQL route handler before the executor is invoked
- [ ] 4.2 Confirm `pln_01starter` and `pln_01growth` receive 403 (no `postgres.admin_sql` flag in `POSTGRES_ADMIN_SQL_PLAN_FLAGS_BY_PLAN`, lines 143-144)
- [ ] 4.3 Confirm `pln_01regulated` and `pln_01enterprise` pass the gate (lines 145-146)

## 5. Injection guard preservation

- [ ] 5.1 Confirm forbidden-pattern checks (`SET ROLE`, `ALTER SYSTEM`, `COPY … PROGRAM`, `BEGIN`/`COMMIT`/`ROLLBACK` — `postgresql-admin.mjs:877-882`) fire before the executor receives the plan
- [ ] 5.2 Confirm `renderPolicyStatement` bounded-declarative check (`postgresql-governance-admin.mjs:187`) fires during plan building for policy bodies
- [ ] 5.3 Confirm executor does NOT re-implement these checks (single responsibility)

## 6. Integration validation

- [ ] 6.1 All new tests pass against `tests/env`
- [ ] 6.2 `bash tests/blackbox/run.sh` — all existing tests still pass
- [ ] 6.3 `openspec validate add-postgres-ddl-execute --strict` passes
