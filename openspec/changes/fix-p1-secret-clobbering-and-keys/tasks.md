## 1. Failing tests

- [ ] 1.1 [test] Add a `helm template charts/realtime-gateway` smoke
      asserting the rendered manifest list contains zero `kind: Secret`
      entries with `stringData.*: ""`; today the test fails on the three
      empty-default keys at `secret-ref.yaml:6-9`.
- [ ] 1.2 [test] Add a smoke that runs `grep -l REPLACE_WITH_BASE64
      deploy/k8s/` and asserts zero matches; today the test fails on
      `encryption-config.yaml:14`.

## 2. Implementation

- [ ] 2.1 [fix] Delete `charts/realtime-gateway/templates/secret-ref.yaml`,
      `charts/workspace-docs-service/templates/secret.yaml`,
      `helm/charts/backup-status/templates/secret.yaml`.
- [ ] 2.2 [fix] Add `existingSecret: <name>` to each affected chart's
      `values.yaml`; update the corresponding `deployment.yaml` `envFrom` to
      reference the `existingSecret`.
- [ ] 2.3 [fix] Add a Helm pre-install hook to each of the three charts that
      uses `lookup` to assert the named Secret exists; fail render when it
      does not.
- [ ] 2.4 [fix] Replace `deploy/k8s/encryption-config.yaml:14` literal with
      a `# generated by scripts/render-encryption-config.sh` placeholder
      sentinel; reject the manifest at apply time when the sentinel is
      still present.
- [ ] 2.5 [impl] Add `scripts/render-encryption-config.sh` that runs `head
      -c 32 /dev/urandom | base64` and writes the rendered manifest to
      `deploy/k8s/encryption-config.rendered.yaml` (gitignored).

## 3. Validation

- [ ] 3.1 [docs] Document the new operator workflow (ESO provisioning
      first, then chart install) in `charts/in-falcone/README.md`.
- [ ] 3.2 [test] Run the `helm template` smokes plus `openspec validate
      fix-p1-secret-clobbering-and-keys --strict`; both green.
