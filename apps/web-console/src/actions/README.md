# Web Console Actions

This directory stores backend-oriented action helpers reserved for future OpenWhisk and control-plane integration work.

These `.mjs` files are **not** part of the React SPA runtime and must not be imported from client-side React components.

For US-UI-01-T01 the React application lives under the rest of `apps/web-console/src/`, while these action stubs remain isolated here for later tasks.
