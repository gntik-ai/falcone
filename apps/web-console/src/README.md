# Web Console

Reserved for the operator-facing frontend.

Expected future concerns:

- authentication and session UX, including login, signup, activation waiting-room flows, and password recovery
- project and environment views
- rollout and health dashboards
- public API route-catalog browsing and filtering through `public-api-catalog.mjs`
