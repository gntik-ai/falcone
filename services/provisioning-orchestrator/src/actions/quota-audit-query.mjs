import { queryQuotaAudit } from '../repositories/quota-audit-repository.mjs';
function auth(params){ const actor=params.callerContext?.actor; if(!actor?.id) throw Object.assign(new Error('Forbidden'), { code:'FORBIDDEN', statusCode:403 }); return actor; }
export async function main(params={}, overrides={}) { const db=overrides.db??params.db; const actor=auth(params); const tenantId = actor.type === 'tenant-owner' ? actor.tenantId : (params.tenantId ?? null); if (actor.type === 'tenant-owner' && params.tenantId && params.tenantId !== actor.tenantId) throw Object.assign(new Error('Forbidden'), { code:'FORBIDDEN', statusCode:403 }); const rows = await queryQuotaAudit(db,{ tenantId, dimensionKey:params.dimensionKey ?? null, actorId:params.actorId ?? null, from:params.from ?? null, to:params.to ?? null }); return { statusCode:200, body:{ entries: rows, total: rows.length } }; }
