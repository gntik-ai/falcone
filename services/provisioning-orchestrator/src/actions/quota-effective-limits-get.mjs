import { resolveEffectiveLimitsForTenant } from '../repositories/quota-enforcement-repository.mjs';
function getAuth(params){ const actor=params.callerContext?.actor; if (!actor?.id) throw Object.assign(new Error('Forbidden'), { code:'FORBIDDEN' }); return actor; }
export async function main(params={}, overrides={}) { const db=overrides.db??params.db; const actor=getAuth(params); const tenantId=params.tenantId ?? actor.tenantId; if (actor.type==='tenant-owner' && tenantId !== actor.tenantId) throw Object.assign(new Error('Forbidden'), { code:'FORBIDDEN', statusCode:403 }); const result=await resolveEffectiveLimitsForTenant(db, tenantId); if (actor.type==='tenant-owner') result.effectiveLimits = result.effectiveLimits.map((item) => ({ ...item, overrideMetadata: undefined })); return { statusCode:200, body:result }; }
