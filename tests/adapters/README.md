# Adapter Integration Test Scaffolding

This directory holds lightweight adapter-integration tests that validate fixture anchoring and adapter-scope expectations before real provider implementations exist.

Future tasks should extend this directory with hermetic integration tests against local doubles, simulators, or ephemeral dependencies.
