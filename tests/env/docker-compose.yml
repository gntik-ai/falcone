# Falcone test environment — REAL backing services for integration tests.
#
# Brings up the in-cluster dependencies that Falcone services talk to, so changes
# can be exercised against real infrastructure (not mocks):
#   - postgres : per-service relational store (backup-status, openapi-sdk, ...)
#   - keycloak : internal IdP. Tenants map 1:1 to realms (realm name == tenantId);
#                a realm's displayName is the authoritative tenant name.
#   - redpanda : Kafka API broker (events, audit, CDC change streams).
#   - ferretdb : document store (MongoDB-wire) over the documentdb (PostgreSQL+DocumentDB)
#                engine. FerretDB has no change streams; realtime/CDC uses Postgres logical replication.
#   - seaweedfs: S3-compatible object storage (workspace SDK artifacts, storage
#                config export); all-in-one server on :58333. Bucket `falcone-test` created on `up`.
#   - vault    : secret store (dev mode). Emits a FILE audit log to a host-mounted
#                path that secret-audit-handler tails (VAULT_AUDIT_LOG_PATH).
#
# Lifecycle is driven by tests/env/up.sh (waits for health, provisions the admin
# service account, applies migrations, brings up the FerretDB+DocumentDB document store, creates
# the SeaweedFS bucket, enables the Vault file audit device) and tests/env/down.sh.
#
# OUT OF SCOPE here (these stay on the Kubernetes/Helm path): OpenWhisk/functions
# (too heavy for compose), the Falcone app + APISIX gateway containers (no
# Dockerfiles — services run in-process in tests), and observability.
name: falcone-testenv

services:
  postgres:
    # pgvector/pgvector:pg16 = official postgres:16 + the pgvector extension preinstalled.
    # Drop-in for the stock image (same POSTGRES_* env, entrypoint, pg_isready); required so
    # the vector-search real-stack probe can `CREATE EXTENSION vector` instead of self-skipping.
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: falcone
      POSTGRES_PASSWORD: falcone
      POSTGRES_DB: falcone_test
    ports:
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U falcone -d falcone_test"]
      interval: 3s
      timeout: 3s
      retries: 20
    tmpfs:
      - /var/lib/postgresql/data  # ephemeral: clean DB every run

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    command: ["start-dev", "--import-realm", "--http-port=8080"]
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HEALTH_ENABLED: "true"
    ports:
      - "8081:8080"
    volumes:
      - ./keycloak/import:/opt/keycloak/data/import:ro
    healthcheck:
      # Keycloak 26 has no curl/wget; probe the master realm over bash /dev/tcp.
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo ok >&3"]
      interval: 5s
      timeout: 4s
      retries: 40

  redpanda:
    image: redpandadata/redpanda:v24.2.7
    command:
      - redpanda
      - start
      - --mode=dev-container
      - --smp=1
      - --kafka-addr=internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr=internal://redpanda:9092,external://localhost:19092
    ports:
      - "19092:19092"
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy:.*true'"]
      interval: 5s
      timeout: 5s
      retries: 40

  # FerretDB gateway — the MongoDB-wire document store, replacing mongo:7 in the
  # MongoDB -> FerretDB cutover (add-ferretdb-data-access-cutover #459). It translates the
  # wire protocol to SQL against the `documentdb` engine below. Same external port (57017)
  # as the replaced mongo:7, so MONGO_URI consumers are unchanged. Engine-first startup:
  # depends_on the documentdb healthcheck so the gateway never connects before the backend
  # (and its documentdb extension) is ready.
  # NOTE: change streams / CDC are NOT supported by FerretDB — realtime tests/env paths are
  # deferred to add-ferretdb-realtime-cdc-remediation (#460). Set MONGO_BACKEND=ferretdb so
  # the data-API rejects `transaction` ops at the boundary (501).
  ferretdb:
    image: ghcr.io/ferretdb/ferretdb:2.7.0@sha256:5706414241eb84f0515512c37b46db0f1b1eac9e5ceb7e4c2523211c184b1985
    environment:
      # Backend DSN -> the documentdb engine's `postgres` database (where the documentdb
      # extension + documentdb_api schema live; the engine has no TLS in tests/env).
      FERRETDB_POSTGRESQL_URL: postgres://falcone:falcone@documentdb:5432/postgres?sslmode=disable
      FERRETDB_LISTEN_ADDR: ":27017"
      FERRETDB_DEBUG_ADDR: ":8088"
    ports:
      - "57017:27017"
    depends_on:
      documentdb:
        condition: service_healthy
    # No container healthcheck: the FerretDB v2.7.0 image is DISTROLESS (no shell, no wget),
    # so a docker-compose CMD healthcheck cannot run. Engine-first ordering is gated by the
    # documentdb healthcheck above (depends_on: service_healthy); gateway readiness is gated
    # by the MongoDB driver's serverSelection retry in the consumers (run-mongo.sh / tests).
    # On Kubernetes the gateway uses a kubelet httpGet probe on /debug/readyz (kubelet-side,
    # no in-container tool needed) — see charts/in-falcone/values.yaml ferretdb.readinessProbe.

  documentdb:
    # DocumentDB engine (FerretDB document-store backend) — MongoDB-wire-compatible
    # Postgres 17 extension stack. Lets real-stack tests target the engine without a
    # Kubernetes cluster (add-ferretdb-documentdb-engine). Pinned by tag + digest to
    # match the Helm chart (charts/in-falcone/values.yaml documentdb.image). Published
    # on 55433 (the shared postgres is 55432) per the 5xxxx host-port convention.
    image: ghcr.io/ferretdb/postgres-documentdb:17-0.107.0-ferretdb-2.7.0@sha256:2386795ec2aa7ae559304361979f1dc5708d383ee9020ae63dadc2940dfe58f7
    # SERVER-START GUCs via the postgres command line so they are active on the FIRST-INIT
    # temp server too — the official postgres entrypoint passes these `-c` args to the temp
    # server that runs docker-entrypoint-initdb.d. A conf.d include file does NOT work here:
    # the temp init server does not read /etc/postgresql/conf.d, so `CREATE EXTENSION documentdb`
    # (which needs pg_documentdb_core preloaded) fails during init. (The Helm chart instead
    # creates the extension via a POST-startup init Job, where the image's bundled preload is
    # already active.) add-ferretdb-data-access-cutover #459 (surfaced on the first live compose run).
    command:
      - postgres
      - -c
      - shared_preload_libraries=pg_cron,pg_documentdb_core,pg_documentdb
      - -c
      - cron.database_name=postgres
      # Logical replication is the realtime/CDC mechanism after the FerretDB cutover
      # (add-ferretdb-realtime-cdc-remediation #460): FerretDB v2 has no change streams, so
      # realtime SSE + the Kafka CDC bridge consume a pgoutput logical replication slot on the
      # documentdb_data tables. wal_level MUST be `logical` (default `replica`), else
      # pg_create_logical_replication_slot fails "logical decoding requires wal_level >= logical".
      - -c
      - wal_level=logical
    environment:
      POSTGRES_USER: falcone
      POSTGRES_PASSWORD: falcone
      # POSTGRES_DB MUST be `postgres`: the engine image ships its own
      # /docker-entrypoint-initdb.d/20-install.sql that runs `CREATE EXTENSION documentdb`
      # against POSTGRES_DB, and the extension only installs in `postgres` (pg_cron is bound
      # to cron.database_name='postgres'). FerretDB also connects to `postgres`. So the image's
      # bundled init creates the extension for us — no custom init SQL is needed here
      # (add-ferretdb-data-access-cutover #459, surfaced on the first live compose run).
      POSTGRES_DB: postgres
      # Keep PGDATA in a subdir of the (tmpfs) data dir, like the chart.
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "55433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U falcone -d postgres"]
      interval: 3s
      timeout: 3s
      retries: 20
    tmpfs:
      - /var/lib/postgresql/data  # ephemeral: clean DB every run

  seaweedfs:
    # All-in-one SeaweedFS (master + volume + filer + S3 gateway) — the dev/test
    # object store that replaces MinIO (ADR-13). Pinned to the spike-validated 4.33
    # digest (also used by tests/env/seaweedfs/run.sh and the Helm chart). The shared
    # compose uses the embedded filer store on tmpfs for robustness; filer-on-PostgreSQL
    # is exercised by the Helm chart and the tests/env/seaweedfs slice, not duplicated here.
    image: chrislusf/seaweedfs@sha256:f0b358973e81f884304737645dd3b278c590c2c9d47d60089729d46324f70495
    command: >
      server -dir=/data -ip=seaweedfs -ip.bind=0.0.0.0 -filer -s3
      -s3.port=8333 -s3.config=/etc/seaweedfs/s3-identities.json
      -master.volumeSizeLimitMB=128 -volume.max=20
    volumes:
      - ./seaweedfs/conf/s3-identities.json:/etc/seaweedfs/s3-identities.json:ro
    ports:
      - "58333:8333"  # S3 API (path-style)
    healthcheck:
      # busybox wget (shipped in the image) probes the S3 gateway status endpoint.
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8333/status || exit 1"]
      interval: 5s
      timeout: 4s
      retries: 40
    tmpfs:
      - /data  # ephemeral object storage

  vault:
    image: hashicorp/vault:1.18
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: root
      VAULT_DEV_LISTEN_ADDRESS: 0.0.0.0:8200
      # Address used by the in-container vault CLI (audit-enable in up.sh).
      VAULT_ADDR: http://localhost:8200
      VAULT_TOKEN: root
    command: ["server", "-dev"]
    ports:
      - "58200:8200"
    volumes:
      # Host-mounted audit dir so a host test process (secret-audit-handler) can
      # tail the file audit log written by the Vault file audit device.
      - ./vault/audit:/vault/audit
    healthcheck:
      # Dev server is auto-unsealed; `vault status` returns 0 when sealed=false.
      test: ["CMD-SHELL", "vault status >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 4s
      retries: 40

  # ---- Temporal (workflow engine) --------------------------------------------
  # Real Temporal dev-server for the workflow-worker real-stack tests
  # (change add-flows-dsl-interpreter-worker / #359): durable worker-kill resume,
  # version pinning, node-ID-from-history, and SDK replay all need a real Temporal
  # server with durable history — an in-memory mock cannot replicate replay.
  #
  # temporalio/auto-setup = the lightest officially-supported single-container dev
  # setup. It needs a SQL backend; temporal-postgres is dedicated to it (kept apart
  # from the app `postgres`/pgvector service so neither pollutes the other). Both use
  # tmpfs — ephemeral, clean every run, consistent with the postgres/mongo pattern.
  # SQL visibility on Postgres (ENABLE_ES=false), per the Temporal Helm decision (#357).
  temporal-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: temporal
      POSTGRES_PASSWORD: temporal
      POSTGRES_DB: temporal
    # Internal only — only the temporal service connects to it.
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U temporal"]
      interval: 3s
      timeout: 3s
      retries: 30
    tmpfs:
      - /var/lib/postgresql/data  # ephemeral

  temporal:
    image: temporalio/auto-setup:1.25.2
    depends_on:
      temporal-postgres:
        condition: service_healthy
    environment:
      # auto-setup defaults: PostgreSQL persistence + SQL (PostgreSQL) visibility.
      DB: postgres12
      DB_PORT: "5432"
      POSTGRES_USER: temporal
      POSTGRES_PWD: temporal
      POSTGRES_SEEDS: temporal-postgres
      # SQL visibility on Postgres — no Elasticsearch (matches the prod Helm decision).
      ENABLE_ES: "false"
      SKIP_DEFAULT_NAMESPACE_CREATION: "false"
      DEFAULT_NAMESPACE: default
      DEFAULT_NAMESPACE_RETENTION: "1d"
    # Frontend gRPC on the host (non-default-clash-safe, matches the spike).
    ports:
      - "7233:7233"
    healthcheck:
      test: ["CMD", "temporal", "operator", "cluster", "health", "--address", "temporal:7233"]
      interval: 5s
      timeout: 5s
      retries: 40

  # ---- HTTP request-chain slice ----------------------------------------------
  # action-runner: TEST-ONLY HTTP shim that adapts plain HTTP -> the OpenWhisk-
  # style product actions (params in / {statusCode,body} out) and injects a real
  # pg Pool. Imports the PRODUCT actions as-is from the bind-mounted repo. Most
  # families trust the gateway-injected identity headers; backup-status instead
  # validates the Bearer JWT itself against KEYCLOAK_JWKS_URL (in-action auth),
  # which is why this container carries DB_URL + KEYCLOAK_JWKS_URL env.
  action-runner:
    build:
      context: ./action-runner
    environment:
      PORT: "8090"
      PGHOST: postgres
      PGPORT: "5432"
      PGUSER: falcone
      PGPASSWORD: falcone
      PGDATABASE: falcone_test
      # backup-status family (in-action JWKS auth + own pg client). The shim
      # primes the .js repository's pg client with its shared pool via setClient
      # (DB_URL is the in-network host/port — NOT the host-published 55432). The
      # action validates the Bearer JWT itself against KEYCLOAK_JWKS_URL (the
      # in-network keycloak:8080, the realm the slice provisions). KEYCLOAK_ISSUER
      # / KEYCLOAK_AUDIENCE are intentionally LEFT UNSET so the action skips those
      # claim checks (the JWKS signature is the real gate); TEST_MODE is NOT set
      # (refused while a JWKS URL is configured); NODE_ENV stays non-production.
      DB_URL: postgres://falcone:falcone@postgres:5432/falcone_test
      KEYCLOAK_JWKS_URL: http://keycloak:8080/realms/falcone-e2e/protocol/openid-connect/certs
      NODE_ENV: test
    volumes:
      # Repo bind-mounted read-only so the shim imports services/ as-is.
      - ../../:/repo:ro
    ports:
      - "8090:8090"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:8090/healthz').then(r=>process.exit(r.status===200?0:1)).catch(()=>process.exit(1))\""]
      interval: 5s
      timeout: 4s
      retries: 20

  # apisix: API gateway. Standalone YAML config provider (no etcd). Validates the
  # Keycloak realm JWT and injects identity headers from JWT claims, then proxies
  # to action-runner. Config in ./apisix/apisix.yaml + ./apisix/config.yaml.
  apisix:
    image: apache/apisix:3.9.1-debian
    # NOTE: APISIX_STAND_ALONE is intentionally NOT set. With it, the entrypoint
    # rewrites config.yaml in place (fails on a bind mount the apisix user cannot
    # write). Our config.yaml already declares role: data_plane + config_provider:
    # yaml, so APISIX runs in standalone YAML mode purely from the mounted config.
    volumes:
      - ./apisix/config.yaml:/usr/local/apisix/conf/config.yaml:ro
      - ./apisix/apisix.yaml:/usr/local/apisix/conf/apisix.yaml:ro
    ports:
      - "9080:9080"
    depends_on:
      action-runner:
        condition: service_healthy
      keycloak:
        condition: service_healthy
    healthcheck:
      # The apisix image has no curl/wget and the default CMD-SHELL is dash
      # (no /dev/tcp). Invoke bash explicitly to probe the proxy listener
      # (port open == data plane accepting connections).
      test: ["CMD", "bash", "-c", "exec 3<>/dev/tcp/127.0.0.1/9080 && echo ok >&3"]
      interval: 5s
      timeout: 4s
      retries: 30
