# Console E2E Scaffolding

This directory captures the minimum executable scaffolding for console actor/state coverage.

Current scope:

- validate required actor states exist
- validate visible/blocked sections are explicit
- validate matrix entries reference console routes and actor states

Deferred to later tasks:

- browser automation
- accessibility assertions
- screenshot/video evidence
- live backend-console flows
