shared_expectations:
  required_host_keys:
    - api
    - console
    - identity
    - realtime
  required_resources:
    - Namespace
    - ConfigMap
    - Deployment
    - Service
  route_prefixes:
    controlPlane: /control-plane
    identity: /auth
    realtime: /realtime
    console: /
  logical_resources:
    - gateway-config
    - control-plane
    - web-console
    - public-exposure

smoke_scenarios:
  - id: SMOKE-DEV-K8S
    environment: dev
    platform: kubernetes
    expected_exposure_kind: Ingress
    expected_hostnames:
      api: api.dev.in-falcone.example.com
      console: console.dev.in-falcone.example.com
      identity: iam.dev.in-falcone.example.com
      realtime: realtime.dev.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Ingress
  - id: SMOKE-DEV-OCP
    environment: dev
    platform: openshift
    expected_exposure_kind: Route
    expected_hostnames:
      api: api.dev.in-falcone.example.com
      console: console.dev.in-falcone.example.com
      identity: iam.dev.in-falcone.example.com
      realtime: realtime.dev.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Route
  - id: SMOKE-SANDBOX-K8S
    environment: sandbox
    platform: kubernetes
    expected_exposure_kind: Ingress
    expected_hostnames:
      api: api.sandbox.in-falcone.example.com
      console: console.sandbox.in-falcone.example.com
      identity: iam.sandbox.in-falcone.example.com
      realtime: realtime.sandbox.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Ingress
  - id: SMOKE-SANDBOX-OCP
    environment: sandbox
    platform: openshift
    expected_exposure_kind: Route
    expected_hostnames:
      api: api.sandbox.in-falcone.example.com
      console: console.sandbox.in-falcone.example.com
      identity: iam.sandbox.in-falcone.example.com
      realtime: realtime.sandbox.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Route
  - id: SMOKE-STAGING-K8S
    environment: staging
    platform: kubernetes
    expected_exposure_kind: Ingress
    expected_hostnames:
      api: api.staging.in-falcone.example.com
      console: console.staging.in-falcone.example.com
      identity: iam.staging.in-falcone.example.com
      realtime: realtime.staging.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Ingress
  - id: SMOKE-STAGING-OCP
    environment: staging
    platform: openshift
    expected_exposure_kind: Route
    expected_hostnames:
      api: api.staging.in-falcone.example.com
      console: console.staging.in-falcone.example.com
      identity: iam.staging.in-falcone.example.com
      realtime: realtime.staging.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Route
  - id: SMOKE-PROD-K8S
    environment: prod
    platform: kubernetes
    expected_exposure_kind: Ingress
    expected_hostnames:
      api: api.in-falcone.example.com
      console: console.in-falcone.example.com
      identity: iam.in-falcone.example.com
      realtime: realtime.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Ingress
  - id: SMOKE-PROD-OCP
    environment: prod
    platform: openshift
    expected_exposure_kind: Route
    expected_hostnames:
      api: api.in-falcone.example.com
      console: console.in-falcone.example.com
      identity: iam.in-falcone.example.com
      realtime: realtime.in-falcone.example.com
    required_resources:
      - Namespace
      - ConfigMap
      - Deployment
      - Service
      - Route
