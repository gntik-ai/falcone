const TOPICS = { hard_blocked: process.env.QUOTA_ENFORCEMENT_KAFKA_TOPIC_HARD_BLOCKED ?? 'console.quota.hard_limit.blocked', soft_grace_allowed: process.env.QUOTA_ENFORCEMENT_KAFKA_TOPIC_SOFT_EXCEEDED ?? 'console.quota.soft_limit.exceeded', soft_grace_exhausted: process.env.QUOTA_ENFORCEMENT_KAFKA_TOPIC_SOFT_EXCEEDED ?? 'console.quota.soft_limit.exceeded' };
export async function emitEnforcementEvent(producer, decision, payload) { const topic = TOPICS[decision]; const event = { eventType: topic ?? null, timestamp: new Date().toISOString(), ...payload }; if (topic && producer?.send) await producer.send({ topic, messages: [{ key: payload.tenantId ?? payload.dimensionKey ?? event.timestamp, value: JSON.stringify(event) }] }); return event; }
